# Reporting Automation

Tiny example that composes a short Markdown report from the synthetic SPS + micro‑survey projects.

- Input: reads from sibling project folders
- Output: `outputs/report.md`

Automate on CI to publish weekly summaries or commit to a `reports/` branch.
